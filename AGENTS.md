# AGENTS.md

## Git 提交规范

- 提交信息必须使用 Conventional Commits 格式：`<type>(<scope>): <description>`。
- `scope` 可选，`description` 使用简洁的祈使句，不以句号结尾。
- 常用 `type`：`feat`、`fix`、`docs`、`style`、`refactor`、`perf`、`test`、`build`、`ci`、`chore`、`revert`。

## 提交操作要求

- 用户明确要求提交时，采用最小流程：确认状态、暂存本次要求的改动、按规范提交、确认提交结果。
- 不因提交请求重复审查 `.gitignore`、遍历无关目录，或运行未要求的构建、测试和其他检查。
- 提交后只需确认提交成功和工作区状态；不影响提交的提示不应反复检查或阻塞提交，在最终回复中简要说明即可。

## 验证要求

- 一般只需要对本次改动执行编译验证；能够成功编译即可视为验证通过。
- 如果本次改动涉及功能测试、端到端测试文件或相关测试配置，则运行对应的测试。
- 未涉及测试文件时，不主动运行具体功能测试、端到端测试或手工验收；具体功能由用户自行验证。
- 默认不启动模拟器或真机进行点击式手工功能验证；只有用户明确要求时才执行。
- 只有用户明确要求、改动涉及相关测试，或编译失败需要定位问题时，才额外运行其他测试或检查。

## 探索边界与停止条件

### 1. 充分即停止

- 当当前证据已经足以直接回答用户问题或判断任务结果时，立即停止探索并回复。
- 不得为了“更有把握”继续检查不会改变结论的依赖版本、缓存、构建工具、历史实现或无关文件。

### 2. 额外检查前置条件

每次额外读取文件或执行命令前，必须先明确：

1. 当前还缺少什么具体事实；
2. 该事实如何改变结论或实现；
3. 如果不会改变结论，则不得继续执行。

### 3. 只查直接相关内容

- 解释、诊断和事实确认类问题，只检查直接相关的最小文件集合。
- 找到足以支持结论的证据后，不得继续扩展为设计评审、重构建议或替代方案调查。
- 发现潜在的后续修改方向，不得视为当前任务的一部分；只在最终回复中说明，除非用户明确要求处理。

### 4. 禁止推测性跟进

- 用户询问“是否存在”“来自哪里”“是否必要”等问题时，只回答该问题。
- 除非用户明确要求，不修改代码或文档，不验证尚未提出的实现方案，不主动追踪依赖、版本和平台细节。

### 5. 探索预算

- 普通事实确认类问题默认最多执行一次定向搜索和一次必要的文件读取。
- 如果在预算内已经得到结论，立即停止调用工具。
